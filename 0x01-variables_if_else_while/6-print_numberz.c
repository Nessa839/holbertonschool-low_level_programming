#include <stdio.h>
/**
 * Main - prints a number of base 10
 * Return: Goes back to zero
 */

int main(void)
{
int number;

number = 48;
while  (number < 58)
{
putchar(number);
number++;
}
putchar(10);

return (0);
}
